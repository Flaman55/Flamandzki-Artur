(function () {
  window.i18nData = window.i18nData || {};
  window.i18nData['en'] = window.i18nData['en'] || {};
  const existing = window.i18nData['en'].page || {};
  window.i18nData['en'].page = Object.assign({}, existing, {
    title: '#22 Smart Home: Security and Automation Architecture — Artur Flamandzki',
    hero_tag: 'PORTFOLIO R&D — PROJECT #22 — PRODUCTION',
    hero_title: 'Smart Home: Security and Automation Architecture',
    hero_sub: 'Power cut-off · Courier protocol · Night lock · Waste API · 2021–2026',
    date: '2021–2026 · embedded systems / IoT · Home Assistant, YAML, Node-RED, REST API · production',
    summary: '<p>Home automation system built around one principle: <strong>security comes from architecture, not software</strong>. The driveway gate and garage have their power completely cut when closed — no automation bug or cyberattack can open them without a physical switch. Integration includes a waste API, PV and heat pump monitoring, presence simulation. Status: in production since 2021.</p>',
    nav_prev: '← #21',
    nav_next: '↑ Projects',
    body: '<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">01 — Power cut-off — security through hardware architecture</div>\n  <div class="proj-box-body">\n    <p>Standard gate control: relay closes contact — gate opens. Problem: if the relay sticks, automation bugs, or the system is hacked — the gate opens without the user\'s consent.</p>\n    <p><strong>Solution:</strong> power to the driveway gate (<code>switch.brama_zasilanie</code>) and garage (<code>switch.garaz_zasilanie</code>) is <strong>completely disconnected when they are closed</strong>. The drive motor has no voltage — a relay bypass is physically impossible.</p>\n    <table class="proj-table"><thead><tr><th>State</th><th>Power</th><th>Action</th></tr></thead>\n    <tbody>\n      <tr><td>Gate closed</td><td><strong>off</strong></td><td>motor without voltage, movement impossible</td></tr>\n      <tr><td>Open commanded</td><td>power on</td><td>then send control signal</td></tr>\n      <tr><td>Gate open</td><td>power active</td><td>state monitoring in panel</td></tr>\n    </tbody></table>\n    <p>The main panel shows the wicket camera <strong>only when power is on</strong> — otherwise the tile is hidden (no spurious camera-disconnected messages).</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">02 — Courier Protocol — autonomous access with time window</div>\n  <div class="proj-box-body">\n    <p>When a courier rings the gate during permitted hours (e.g. 8:00–18:00), the system runs the courier protocol:</p>\n    <ul class="proj-list">\n      <li>Doorbell activates timer: <code>timer.zamknij_furtke</code> starts (e.g. 3 minutes)</li>\n      <li>Wicket opens: <strong>not the full gate</strong> — wicket mode only</li>\n      <li>Camera appears in living-room panel: live view <code>camera.furtka_live_view</code></li>\n      <li>Indoor doorbell notification in living room (<code>binary_sensor.dzwonek</code>)</li>\n      <li>After timer expires or manual confirmation: wicket closes automatically</li>\n    </ul>\n    <p>Outside permitted hours: doorbell signals only — <strong>nothing opens</strong>. Edge cases: if gate is already open — timer does not start; if night mode — courier protocol is blocked.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">03 — Night Lock · Presence Simulation · Energy Monitoring</div>\n  <div class="proj-box-body">\n    <p><strong>Night Lock:</strong> from a set hour all automations are disabled. A deliberate design decision — at night the gate can only open via a physical remote or touch panel. Disabling automations also prevents remote opening via software bugs or external triggers.</p>\n    <p><strong>Presence simulation</strong> (holiday away mode): scripts turn lights on in various rooms according to a pseudo-random schedule, mimicking occupant activity. Garage lights turn on automatically when the garage door opens, then go off after a few minutes.</p>\n    <p><strong>Energy monitoring</strong> — three independent measurement circuits in one view:</p>\n    <ul class="proj-list">\n      <li>PV installation: <code>sensor.licznik_pv_total_energy</code> — total production [kWh]</li>\n      <li>Grid (consumption): <code>sensor.licznik_energii_total_energy</code> — grid draw</li>\n      <li>Heat pump: <code>water_heater.pompa_ciepla</code> — DHW + control; zones <code>zone_1/2/3</code></li>\n    </ul>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">04 — City data integration — waste schedule from API</div>\n  <div class="proj-box-body">\n    <p>Instead of manually entering collection dates, the system automatically fetches data from the city service and creates sensors:</p>\n    <ul class="proj-list">\n      <li><code>sensor.odpady_niesegregowane</code> — mixed waste (black bin)</li>\n      <li><code>sensor.odpady_papier</code> — paper and cardboard (blue)</li>\n      <li><code>sensor.odpady_plastik</code> — plastic/metal (yellow)</li>\n      <li><code>sensor.odpady_szklo</code> — glass (green)</li>\n      <li><code>sensor.odpady_bio</code> — bio (brown)</li>\n    </ul>\n    <p>Data fetched monthly: schedule in JSON format stored in <code>input_text</code>. Morning automation checks: if today is a collection day — a notification and panel tile appear.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--result">\n  <div class="proj-box-title">A system, not a gadget — in production since 2021</div>\n  <div class="proj-box-body">\n    <ul class="proj-list">\n      <li>Home Assistant as an integration platform, not a bundle of ready-made plugins</li>\n      <li><strong>Security through architecture:</strong> power cut-off instead of relay — no software bug or cyberattack can open the gate when it is closed</li>\n      <li><strong>Night lock</strong> as a conscious trade of convenience for security</li>\n      <li><strong>Courier protocol</strong> as a mini-FSM built from timers and time conditions</li>\n      <li>External data (city API) supplements local sensors</li>\n      <li>Project continuously developed since 2021 — <strong>current status: production</strong></li>\n    </ul>\n  </div>\nv>\n  </div>\n</div>',
  });
})();
