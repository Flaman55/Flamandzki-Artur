(function () {
  window.i18nData = window.i18nData || {};
  window.i18nData['en'] = window.i18nData['en'] || {};
  const existing = window.i18nData['en'].page || {};
  window.i18nData['en'].page = Object.assign({}, existing, {
    title: '#11 AutoMobilClub Łódź Timing System — Artur Flamandzki',
    hero_tag: 'PORTFOLIO R&amp;D — PROJECT #11 — COMMERCIAL CONTRACT',
    hero_title: 'AutoMobilClub Łódź Timing System',
    hero_sub: 'Distributed timing system for motorsport events · two timers in one ISR · false start detection · ATtiny2313 · 2011',
    date: '2011 · commercial contract · ATtiny2313, BASCOM BASIC, RS232 · predecessor to DELFI Racing #05',
    summary: '<p>Commission from AutoMobilClub Łódź: electronic timing system for motorsport events. Requirements: start sequence with lights (5 red + green), lap time precision 0.01 s, driver reaction time 0.001 s, false-start detection, RS232 interface to stopwatch and PC. Core: ATtiny2313 with 2 KB flash — almost entirely full.</p>',
    nav_prev: '← #10',
    nav_next: '#12 →',
    body: '<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">01 — Two independent timers in one ISR: lap time + reaction time simultaneously</div>\n  <div class="proj-box-body">\n    <p>Timer1, prescaler 64, reload <code>65411</code> (at 8 MHz gives exactly <strong>1 ms/tick</strong>). One overflow interrupt serves two independent counters:</p>\n    <ul class="proj-list">\n      <li><strong>Lap time</strong> (<code>Licz = 1</code>): 1 ms → 10 ms → 1 s → 1 min. Precision 10 ms (0.01 s).</li>\n      <li><strong>Reaction time</strong> (<code>Licz_reakcje = 1</code>): 1 ms → 10 ms → 1 s. Precision 1 ms (0.001 s).</li>\n    </ul>\n    <p>Both counters start at different moments: reaction time starts when the green light comes on, lap time — when the start barrier is broken. Independent enable bits allow them to run simultaneously or independently without extra hardware.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">02 — False start detection: Falstart bit in the light sequence</div>\n  <div class="proj-box-body">\n    <p>During the start-light sequence (5 red lights, 750 ms apart) the controller monitors the start photo-barrier. If the barrier is broken before the green light — the <code>Falstart</code> bit is set.</p>\n    <p>The bit does not stop the current measurement — the run continues. Only after time is stopped at the finish is the result displayed with the additional <code>FALSTART</code> flag. <strong>The decision to disqualify rests with the judges; the electronics only records the fact.</strong></p>\n    <p>Implementation: one conditional line inside the light-sequence loop, no extra state variable, no sequence interruption.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">03 — Watchdog reset as memory optimisation: return to start without Goto</div>\n  <div class="proj-box-body">\n    <p>ATtiny2313 has 2 KB of flash — the controller had it <strong>almost entirely full</strong>. Problem: after a measurement ends the program needs to return to the beginning.</p>\n    <p>Standard approach (<code>Goto Poczatek_programu</code>) requires re-initialising all variables. Alternative: <strong><code>Start Watchdog</code></strong> — enabling the watchdog which resets the entire microcontroller after ~16 ms. A hardware reset clears all registers and RAM more thoroughly than a software initialisation.</p>\n    <p>Side effect: the "measurement complete" path is a <strong>dead end</strong> — after displaying the result the program <em>waits</em> for the reset. A few dozen bytes of flash saved on the jump and initialisation.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">04 — Two RS232 outputs: synchronous control of two starting-light sets</div>\n  <div class="proj-box-body">\n    <p>Two complete starting-light sets can be connected (one on each side of the track). ATtiny2313 has one hardware UART; the second serial port is implemented on PortB (software UART via BASCOM). Light sequence sent simultaneously to both ports: <code>Print #1 , Chr(i) : Print #2 , Chr(i)</code>.</p>\n    <p>Bit-sequence encoding: 1→2→4→8→16 (successive bits of the light-controller\'s port B), 750 ms apart. After the sequence ends: <code>Chr(0)</code> extinguishes all LEDs.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">05 — System evolution: V1 → V2 in response to requirements</div>\n  <div class="proj-box-body">\n    <p><strong>Version 1 (KZK V1.1):</strong> photo-barrier controller + stopwatch. Start/stop button, two LEDs, two stopwatch outputs. No lights, no PC, no reaction-time measurement.</p>\n    <p><strong>Version 2 (final):</strong> full system, two operating modes:</p>\n    <ul class="proj-list">\n      <li><strong>Autonomous mode (with stopwatch):</strong> controller sends pulses to the stopwatch via dedicated pins. No PC required.</li>\n      <li><strong>PC mode:</strong> controller measures time itself and sends results via RS232. Competitor number entered from keyboard, results displayed in terminal. Format: <code>czas: MM:SS.dd; czas reakcji: S.mmm [FALSTART]</code>.</li>\n    </ul>\n    <p>"Unplanned" expansion — requirements grew alongside hardware capabilities.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--result">\n  <div class="proj-box-title">Result — system delivered, predecessor to DELFI Racing</div>\n  <div class="proj-box-body">\n    <p>System delivered and used by AutoMobilClub Łódź. Project documented on Elektroda.pl in two iterations as an example of embedded system evolution under growing requirements.</p>\n    <p>Key finding: <strong>ATtiny2313 at full capacity forces unconventional approaches</strong> — Watchdog reset instead of an initialisation loop is not a hack, it is an engineering decision under a hard resource constraint.</p>\n    <p>Experience from this project fed directly into project #05 (DELFI Racing Starting Signal RT1/RT2), where the timing-system architecture was designed from the outset with extensibility in mind.</p>\n  </div>\n</div>',
  });
})();
