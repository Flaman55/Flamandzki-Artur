(function () {
  window.i18nData = window.i18nData || {};
  window.i18nData['en'] = window.i18nData['en'] || {};
  const existing = window.i18nData['en'].page || {};
  window.i18nData['en'].page = Object.assign({}, existing, {
    title: '#19 FochBot: Autonomous AI Assistant — Artur Flamandzki',
    hero_tag: 'PORTFOLIO R&D — PROJECT #19 — ACTIVE R&D',
    hero_title: 'FochBot: Autonomous AI Assistant',
    hero_sub: 'FastAPI · Microservices · BCRYPT · LLM Middleware · Python · 2026',
    date: '2026 · AI / backend · Python, FastAPI, Docker · active R&D',
    summary: '<p>AI system developed across three architectural generations: V1 (intent classifier), V2 (microservices, BCRYPT, user roles), V3 (<strong>dual-layer memory architecture</strong> — Layer A: persistent memory map, Layer B: autonomous controller). Core thesis: an LLM is a stateless neuron; FochBot is the brain with memory.</p>',
    nav_prev: '← #18',
    nav_next: '#20 →',
    body: '<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">V1 — Intent classifier and NLP</div>\n  <div class="proj-box-body">\n    <p>First version: local assistant based on <strong>intent classification</strong>.</p>\n    <p><strong>V1 architecture:</strong></p>\n    <ul class="proj-list">\n      <li><code>intents.json</code> — intent database with example sentences</li>\n      <li><code>core/nlp.py</code> — tokenisation and text cleaning</li>\n      <li><code>core/classifier.py</code> — intent classifier (ML logic), <code>training_data.pkl</code></li>\n      <li><code>core/responder.py</code> — response generation based on classified intent</li>\n      <li><code>interface/cli.py</code> — text interface; <code>interface/web.py</code> — optional web frontend (Flask/Gradio)</li>\n      <li><code>core/arr.py</code> — reserved for MR rhythm analysis integration (slot for future development)</li>\n    </ul>\n    <p>Conversation logs and debug output saved locally; model runs fully offline.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">V2 — Microservices, authentication and roles</div>\n  <div class="proj-box-body">\n    <p>Full rebuild on a <strong>microservices</strong> architecture. Two independent services communicating over an internal network:</p>\n    <table class="proj-table"><thead><tr><th>Service</th><th>Responsibility</th></tr></thead>\n    <tbody>\n      <tr><td><code>API :8000</code></td><td>Business logic: chat, bot responses, client endpoints</td></tr>\n      <tr><td><code>Auth :8001</code></td><td>Identity: sessions, HttpOnly cookies, device tokens, roles</td></tr>\n    </tbody></table>\n    <p><strong>Authorisation flow:</strong> every request to <code>:8000/chat</code> passes through <code>require_auth()</code> — the <code>access_control.py</code> bridge sends <code>POST /check_access</code> to <code>:8001</code>, which checks the session in RAM and returns <code>allowed: true/false</code> with user data.</p>\n    <p><strong>JSON database (local files):</strong> <code>users.json</code> (users, BCRYPT hashes, roles: admin/user/device), <code>devices.json</code> (authorised devices, IP whitelist, tokens), <code>data_manager.py</code> — the only module with write access; <strong>atomic writes</strong> resilient to mid-operation failure. Management via CLI (<code>user_tool.py</code>). Frontend: <code>fochbot-frontend-v2</code>. Delivered in three phases.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">V3 — LLM middleware and autonomous decision-making</div>\n  <div class="proj-box-body">\n    <p><strong>Fundamental principle of V3:</strong> an LLM (GPT, Claude, etc.) is a <em>neuron</em> — a single-shot computational unit. It receives an impulse, generates output, and goes dark. It holds no state and has no memory of its previous call. <strong>FochBot is the brain</strong>: it maintains continuity, memory, and decides when and how to invoke the LLM.</p>\n    <p><strong>Layer A — Memory Map (context):</strong> three-level on-disk archive — raw sessions (<code>session_*.json</code>), index (compact catalogue for the LLM), and knowledge fragments (blocks loaded selectively per query). The LLM always receives only as much context as it needs — no full history.</p>\n    <p><strong>Layer B — Autonomous Controller (decision-making):</strong> makes <strong>local decisions without the LLM</strong> (routing, completeness assessment, query package building), then calls the LLM as a tool. Evaluates the result, optionally initiates re-invocation or escalation. Continuity logic on the FochBot side, not the LLM side.</p>\n    <p><strong>V3 tech stack:</strong> FastAPI backend (<code>app/backend/</code>), frontend (<code>app/frontend/</code>), state management (<code>state/</code>), full technical documentation (FochBot_V3_Dokumentacja.tex/.pdf/.docx).</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--result">\n  <div class="proj-box-title">Project status — active R&amp;D, 2026</div>\n  <div class="proj-box-body">\n    <ul class="proj-list">\n      <li><strong>V1</strong> — intent classifier, offline NLP, conversation logging</li>\n      <li><strong>V2</strong> — microservices (FastAPI), BCRYPT, sessions, roles, frontend, delivered in 3 phases</li>\n      <li><strong>V3</strong> — LLM middleware: Memory Map + Autonomous Controller; remote code browsing, downloading and editing, service stop/restart, and frontend build execution with real-time progress monitoring</li>\n      <li>Open project: integration with MR-AI analysis (project #20), development of Layer B toward full autonomy</li>\n    </ul>\n  </div>\n</div>',
  });
})();
