(function () {
  window.i18nData = window.i18nData || {};
  window.i18nData['en'] = window.i18nData['en'] || {};
  const existing = window.i18nData['en'].page || {};
  window.i18nData['en'].page = Object.assign({}, existing, {
    title: '#09 Divisor Algorithm: Rediscovering a Number Theory Theorem on ATmega8 — Artur Flamandzki',
    hero_tag: 'PORTFOLIO R&amp;D — PROJECT #09 — ALGORITHMIC RESEARCH',
    hero_title: 'Divisor Algorithm: Rediscovering a Number Theory Theorem on ATmega8',
    hero_sub: 'From an algorithmic task to independently deriving a mathematical formula · ATmega8, 1 MHz · 2013',
    date: '2013 · algorithmic research / number theory · ATmega8, BASCOM BASIC, C# · V1.0→V2.0',
    summary: '<p>Task: find a number with exactly 500 divisors. Platform: ATmega8, 1 MHz, BASCOM BASIC. In the process the author empirically derived a classical number-theory formula for constructing the minimal number with a given divisor count, and developed a four-path O(√N) algorithm far faster than the conventional approach. The project became the direct reason for pursuing an engineering degree.</p>',
    nav_prev: '← #08',
    nav_next: '#10 →',
    body: '<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">01 — Four-path divisor-counting algorithm: O(N) → O(√N)</div>\n  <div class="proj-box-body">\n    <p>The conventional method checks all numbers from 1 to N. For N=240 that is 240 modulo divisions. The author discovered that divisors always appear in pairs (d, N/d) and it suffices to search up to √N: for N=240 only the first <strong>16 numbers instead of 240</strong> (saving: 93.3% of operations).</p>\n    <p>The algorithm has four separate paths:</p>\n    <ul class="proj-list">\n      <li><strong>Numbers with an even number of divisors</strong> (general case): each divisor d &lt; √N determines a pair (d, N/d); the counter grows by two.</li>\n      <li><strong>Perfect squares</strong> (odd number of divisors, e.g. N=16): the middle divisor √N counts once; the algorithm must detect when d²=N and add only one.</li>\n      <li><strong>Product of two primes</strong> (exactly 4 divisors, e.g. N=1527=3×509): after finding the first divisor 3, we don\'t check all numbers up to 1527 — the program verifies whether 509 is prime (checks up to √509≈23), finishing after examining <strong>63 numbers instead of 1527</strong>.</li>\n      <li><strong>Prime numbers:</strong> check up to √N; if divisor count equals 2 — done.</li>\n    </ul>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">02 — Independent derivation of the formula: minimal number with D divisors</div>\n  <div class="proj-box-body">\n    <p>Number theory formula: divisor count τ(n) = ∏(aᵢ+1) for n = ∏pᵢ^aᵢ. Given D, one can construct the <strong>smallest</strong> number with exactly D divisors without brute-force.</p>\n    <p>Algorithm:</p>\n    <ol class="proj-list">\n      <li>Factorise D into prime factors (in descending order).</li>\n      <li>Each factor fᵢ corresponds to exponent aᵢ = fᵢ − 1.</li>\n      <li>Assign exponents (descending) to successive primes (ascending).</li>\n    </ol>\n    <p>Example for D=500: 500 = 5×5×5×2×2, exponents [4,4,4,1,1], assigned to [2,3,5,7,11]:</p>\n    <p><code>W = 2⁴·3⁴·5⁴·7¹·11¹ = 16·81·625·7·11 = 62,370,000</code>, τ(W) = 5·5·5·2·2 = 500 ✓</p>\n    <p>The author derived this formula <strong>empirically, without access to the literature</strong>, implementing it as a <code>Wyznacz</code> subroutine in BASCOM BASIC with a prime table. This is a classical number-theory result, here rediscovered through computational experiment.</p>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">03 — ATmega8 implementation: timing precision as proof of correctness</div>\n  <div class="proj-box-body">\n    <p>Hardware constraints forced concrete engineering decisions:</p>\n    <ul class="proj-list">\n      <li><strong>10 ms precision timer</strong> (Timer1, prescaler 8): two independent time counters — one for the main search (Pomiar1), one for the Wyznacz subroutine (Pomiar2). Result on LCD: <code>HH:MM:SS,100ms 10ms</code>.</li>\n      <li><strong>Comparison with the conventional approach:</strong> separate file <code>konwencjonalna zasada.bas</code> — brute-force O(N) as baseline. Timing results on the same platform provide an empirical proof of the speedup.</li>\n      <li><strong>32-bit limit:</strong> the number with 860 divisors is 4,156,153,952,993,280 — beyond DWORD range on AVR. Discovering the hardware boundary became the natural scope of the project.</li>\n      <li><strong>Version history:</strong> V1.0 → V1.34 → V2.0 — each iteration a new algorithm optimisation confirmed by hardware timing measurements.</li>\n    </ul>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">04 — PC tool: calcLiczbaDzielnikow — C# verifier with prime table</div>\n  <div class="proj-box-body">\n    <p>After discovering the 32-bit boundary on ATmega, the author ported the algorithm to PC as a C# application. Goal: result verification beyond DWORD range and comparison with microcontroller output.</p>\n    <ul class="proj-list">\n      <li><strong>Prime table (up to ≈3797)</strong> embedded directly in code — not computed at runtime, eliminating the Sieve of Eratosthenes cost.</li>\n      <li><strong><code>BascomWyznacz()</code> method</strong> — faithful port of the BASCOM Wyznacz subroutine: same D factorisation algorithm, same exponent assignment order. Uses <code>ulong</code> (64 bits), extending the range 4× versus AVR.</li>\n      <li><strong>Recursive <code>WyznaczLiczbe()</code> and <code>WyznaczDzielniki()</code></strong> — prime factorisation of W as inverse verification: for the computed number W the program checks whether τ(W) = D.</li>\n    </ul>\n  </div>\n</div>\n\n<div class="proj-box proj-box--innovation">\n  <div class="proj-box-title">05 — PC tool: BigInteger/makeclass — arbitrary-precision arithmetic for W > 2³²</div>\n  <div class="proj-box-body">\n    <p>Numbers with a large divisor count quickly exceed the 32-bit range — for D=860 the result is 4,156,153,952,993,280 ≈ 4.2×10¹⁵, beyond DWORD. The author implemented a custom arbitrary-precision arithmetic library in C#.</p>\n    <ul class="proj-list">\n      <li><strong>Chunk multiplication (<code>Extract.BuildALU()</code>):</strong> number split into blocks of <code>up</code> digits; product computed by summing partial products with positional shift — identical principle to long multiplication, implemented on <code>ulong</code>.</li>\n      <li><strong><code>Digit()</code> / <code>DigitNewMethod()</code> methods</strong> — assembling the result from chunks with carry handling; output compared against <code>System.Numerics.BigInteger</code> (.NET) as ground truth.</li>\n      <li><strong><code>makeclass</code> project</strong> uses this library as the computation engine: given D, the result W is computed with full precision regardless of 32- or 64-bit limits.</li>\n    </ul>\n  </div>\n</div>\n\n<div class="proj-box proj-box--result">\n  <div class="proj-box-title">Result and impact — algorithmic task → mathematical research → university studies</div>\n  <div class="proj-box-body">\n    <p>An algorithmic task from a class the author attended as a guest listener turned into independent mathematical research.</p>\n    <p>The project became the <strong>direct reason for pursuing higher education</strong> — the author persuaded the owner of Laskomex to fund both the engineering and master\'s degree programmes, which he completed.</p>\n    <p>Proof that an experimental approach to a problem (MCU implementation as research environment) can lead to the discovery of mathematical theorems independently of a formal academic path.</p>\n  </div>\n</div>',
  });
})();
